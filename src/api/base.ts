export interface SimpleDepartment {
  id: number;
  depName: string;
}

export interface SimpleEmployee {
  id: number;
  name: string;
}

export interface SimpleRole {
  roleCode: string;
  roleName: string;
}

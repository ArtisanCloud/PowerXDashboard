import { PageContainer } from '@ant-design/pro-components';

const SetupMenu: React.FC = () => {
  return (
    <PageContainer
      header={{
        title: '员工角色配置',
      }}
    ></PageContainer>
  );
};
export default SetupMenu;
